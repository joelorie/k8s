apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: logoutputapp-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: logoutputapp-svc
                port:
                  number: 2345
